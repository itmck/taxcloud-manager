<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <!--配置别名 可以不进行配置此项
        配置此项目的,可以不使用类的全限定名,而是直接使用类的名字(忽略首字母大小写)
    -->
    <typeAliases>
        <package name="com.syun.pojo"/>
    </typeAliases>

</configuration>